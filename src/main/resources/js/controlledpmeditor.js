/*
AJS.$(document).ready(function() {
    console.log("!!! DKOUDELA AJS CALLED !!!");
    //AJS.$("#theCurrentDate").set("datetime", "2001-01-01");
    /*
    var allSpanElements = $("span");
    console.log("DKOUDELA: spans: " + allSpanElements);
    console.log("DKOUDELA: spans len: " + allSpanElements.length);
    var theCurrentDate = [
        '<time onselectstart="return false;" class="non-editable" datetime="2015-01-01" contenteditable="false">',
        '01 Jan 2015',
        '</time>'
    ];
    var inner = theCurrentDate.join("");
    allSpanElements.each(function( i, val ) {
        console.log("DKOUDELA: span: " + val);
        console.log("DKOUDELA: span text: " + val.text());
        console.log("DKOUDELA: span html: " + val.htmlText());
        if (this.text == "Span") {
            this.html(inner)
        }
    });
*/
//    $("span:contains('Span')").innerHTML(inner);
/*
    var span;
    $('span').each(function(){
        console.log("DKOUDELA: span VRRRRR : " + $(this).text());
        if($(this).html() == 'Span'){
            console.log("DKOUDELA: span FOUND !!!: " + $(this));
            span = $(this);
        }
    });
*//*
    console.log("!!! DKOUDELA AJS ENDED !!!");
});
*/